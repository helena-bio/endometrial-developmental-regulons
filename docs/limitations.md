# Limitations

This repository records the completed computational analyses. It does not
redistribute every upstream data object used during execution.

The analyses retain the cohort definitions, model boundaries, and software
records present in the final source packages. The repository structure does
not introduce a broader scientific claim.
